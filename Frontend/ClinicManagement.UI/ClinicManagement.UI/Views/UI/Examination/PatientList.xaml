<UserControl x:Class="ClinicManagement.UI.Views.UI.Examination.PatientList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ClinicManagement.UI.Views.UI.Examination"
             xmlns:components="clr-namespace:ClinicManagement.UI.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="800">

    <Grid>
        <Border BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="15"
                Background="{StaticResource CardBrush}">

            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Danh sách khám"
                               FontSize="20"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Ngày khám: " FontSize="13" Opacity="0.6"/>
                        <TextBlock Text="{Binding NgayKhamText}" FontSize="13" FontWeight="Medium" Foreground="DeepSkyBlue"/>
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Text="{Binding CounterText}"
                           FontSize="16"
                           Foreground="Green"
                           Margin="10,0,0,15"/>

                <Border Grid.Row="2"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Padding="15,10"
                        CornerRadius="10"
                        BorderBrush="{StaticResource BorderBrush}"
                        BorderThickness="1">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="10,0,10,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="STT" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.7"/>
                            <TextBlock Grid.Column="1" Text="Họ Tên" FontWeight="Bold" Margin="10,0,5,0" Opacity="0.7"/>
                            <TextBlock Grid.Column="2" Text="Giới Tính" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.7"/>
                            <TextBlock Grid.Column="3" Text="Năm Sinh" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.7"/>
                            <TextBlock Grid.Column="4" Text="Địa Chỉ" FontWeight="Bold" Margin="5,0" Opacity="0.7"/>
                            <TextBlock Grid.Column="5" Text="Trạng Thái" FontWeight="Bold" HorizontalAlignment="Center" Opacity="0.7"/>
                        </Grid>

                        <ListBox Grid.Row="1" 
                                 ItemsSource="{Binding UiPatientsList}"
                                 SelectedItem="{Binding SelectedPatientItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 HorizontalContentAlignment="Stretch"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto">

                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Margin" Value="0,0,0,4"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border Background="#01000000" SnapsToDevicePixels="True">
                                                    <ContentPresenter />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>

                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <components:PatientListItem />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Grid>
                </Border>

                <Button Grid.Row="3"
                        Content="Thêm bệnh nhân"
                        Width="150"
                        Height="35"
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0"
                        Style="{StaticResource PrimaryButton}"
                        Command="{Binding GoToFormCommand}"
                        Visibility="{Binding IsAddButtonVisible}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>