<UserControl x:Class="ClinicManagement.UI.Views.UI.Patient.PrescriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:ClinicManagement.UI.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="800">

    <Grid Margin="10">
        <Border BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="15"
                Background="{StaticResource CardBrush}"
                Padding="15">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- TITLE -->
                <TextBlock Grid.Row="0"
                           Text="Phiếu khám bệnh"
                           FontSize="22"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- FORM -->
                <Grid Grid.Row="1" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <TextBlock Text="Ngày khám"/>
                        <DatePicker Margin="0,5,0,10"/>

                        <TextBlock Text="Họ tên"/>
                        <TextBox Margin="0,5,0,10"/>

                        <TextBlock Text="Triệu chứng"/>
                        <TextBox Margin="0,5,0,10"/>

                        <TextBlock Text="Chẩn đoán"/>
                        <TextBox Margin="0,5,0,10"/>

                        <TextBlock Text="Loại bệnh"/>
                        <ComboBox Margin="0,5,0,10"/>
                    </StackPanel>
                </Grid>

                <Separator Grid.Row="2" Margin="0,5"/>

                <!-- MEDICINE -->
                <Grid Grid.Row="3">

                    <TextBlock Text="Đơn thuốc"
                               FontSize="18"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,15"/>

                    <!-- HEADER -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Text="Thuốc"/>
                        <TextBlock Grid.Column="1" Text="Đơn vị" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Text="Số lượng" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="3" Text="Cách dùng" HorizontalAlignment="Center"/>
                    </Grid>

                    <!-- LIST -->
                    <ScrollViewer 
                                  VerticalScrollBarVisibility="Auto">

                        <ItemsControl>
                            <ItemsControl.Items>
                                <components:MedicineRow/>
                                <components:MedicineRow/>
                            </ItemsControl.Items>
                        </ItemsControl>

                    </ScrollViewer>

                    <Button Content="Thêm"
                            Width="90"
                            Margin="0,15,0,0"
                            HorizontalAlignment="Left"
                            Style="{StaticResource CancelButton}"/>
                </Grid>

                <!-- BUTTONS -->
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">

                    <Button Content="Hoàn tất"
                            Margin="10,0"
                            Style="{StaticResource PrimaryButton}"/>

                    <Button Content="Hủy"
                            Margin="10,0"
                            Style="{StaticResource CancelButton}"/>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>