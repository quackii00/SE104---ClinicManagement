<UserControl x:Class="ClinicManagement.UI.Views.Components.PatientListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClinicManagement.UI.Views.Components"
             xmlns:services="clr-namespace:ClinicManagement.UI.Services"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">

    <Border Margin="0,2"
            Padding="8"
            CornerRadius="8"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            Background="{StaticResource CardBrush}">

        <Grid Background="Transparent" IsHitTestVisible="True">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding STT}" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold" FontSize="14" Foreground="DeepSkyBlue"/>
            <TextBlock Grid.Column="1" Text="{Binding BenhNhan.HoTen}" IsHitTestVisible="False" VerticalAlignment="Center" FontSize="14" Margin="10,0,5,0" TextTrimming="CharacterEllipsis"/>
            <TextBlock Grid.Column="2" Text="{Binding BenhNhan.GioiTinh}" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14"/>
            <TextBlock Grid.Column="3" Text="{Binding BenhNhan.NamSinh}" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="14"/>
            <TextBlock Grid.Column="4" Text="{Binding BenhNhan.DiaChi}" IsHitTestVisible="False" VerticalAlignment="Center" FontSize="14" Margin="5,0" TextTrimming="CharacterEllipsis"/>

            <ComboBox Grid.Column="5"
                      Width="110"
                      Height="28"
                      VerticalAlignment="Center"
                      SelectedValue="{Binding TrangThai, Mode=TwoWay}"
                      SelectedValuePath="Content"
                      IsHitTestVisible="False"
                      Focusable="False">
                <ComboBox.Style>
                    <Style TargetType="ComboBox">
                        <!-- Mặc định (trạng thái khác) -->
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Foreground" Value="#555555"/>
                        <Setter Property="Background" Value="#EEEEEE"/>
                        <Style.Triggers>
                            <!-- Chờ khám -> vàng -->
                            <DataTrigger Binding="{Binding TrangThai}" Value="Chờ khám">
                                <Setter Property="Background" Value="#FFF3CD"/>
                                <Setter Property="Foreground" Value="#8A6D00"/>
                            </DataTrigger>
                            <!-- Đang khám -> xanh dương -->
                            <DataTrigger Binding="{Binding TrangThai}" Value="Đang khám">
                                <Setter Property="Background" Value="#CCE5FF"/>
                                <Setter Property="Foreground" Value="#084298"/>
                            </DataTrigger>
                            <!-- Đã khám -> xanh lá -->
                            <DataTrigger Binding="{Binding TrangThai}" Value="Đã khám">
                                <Setter Property="Background" Value="#D4EDDA"/>
                                <Setter Property="Foreground" Value="#1E7E34"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ComboBox.Style>
                <ComboBoxItem Content="Chờ khám"/>
                <ComboBoxItem Content="Đang khám"/>
                <ComboBoxItem Content="Đã khám"/>
            </ComboBox>

            <Button Grid.Column="6"
                    Content="✕"
                    Width="28"
                    Height="28"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontWeight="Bold"
                    FontSize="16"
                    Foreground="Red"
                    Command="{Binding DataContext.XoaBenhNhanCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                    CommandParameter="{Binding}">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Source={x:Static services:AppState.Instance}, Path=CurrentUserRole}" Value="Tiếp Tân">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>

                            <DataTrigger Binding="{Binding TrangThai}" Value="Đã khám">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</UserControl>